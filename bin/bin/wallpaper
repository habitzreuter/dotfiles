#!/usr/bin/env python3
# Idea from https://git.sr.ht/~sircmpwn/dotfiles/tree/master/bin/wallpaper
import glob
import random
from os.path import expanduser

home = expanduser("~")
images = list()
images += glob.glob(home + "/Pictures/wallpapers/*")

print(random.choice(images))
